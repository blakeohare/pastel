
string intArrayToString(Array<int> arr) {
    List<string> sb = new List<string>();
    for (int i = 0; i < arr.size(); i += 1) {
        sb.add(Convert.intToString(arr[i]));
    }
    return sb.join(" ");
}
