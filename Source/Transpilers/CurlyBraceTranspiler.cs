namespace Pastel.Transpilers
{
    internal abstract class CurlyBraceTranspiler : AbstractTranspiler
    {
        public CurlyBraceTranspiler(TranspilerContext transpilerCtx)
            : base(transpilerCtx)
        { }
    }
}
